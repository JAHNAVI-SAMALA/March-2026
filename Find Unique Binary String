class Solution:
    def findDifferentBinaryString(self, nums: List[str]) -> str:
        res  = ""
        n = len(nums)
        for i in range(n):
            if nums[i][i] == "0":
                res += "1"
            else:
                res += "0"
        return res
