class Solution:
    def bitwiseComplement(self, n: int) -> int:
        s = bin(n)[2:]
        res = ""
        for c in s:
            if c == "0":
                res += "1"
            else:
                res += "0"
        return int(res,2)
        
